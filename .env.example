# Story Chain Docker 环境变量配置
# 复制此文件为 .env 并修改配置

# JWT 密钥（请在生产环境中修改为强密码）
JWT_SECRET=story-chain-secret-key-change-in-production

# 前端服务端口（宿主机映射端口）
FRONTEND_PORT=80

# 后端服务端口（宿主机映射端口，用于直接调试）
BACKEND_PORT=8080
